<%- include('../partials/boilerplate') %>

<div class="todo-wrapper">
  <!-- we declare the browser live route -->
    <a href="/launches/new" class="buttonlike">Add a Launch</a>
    <% launches.forEach(launch => { %>
      <div class="todo">
        <div class="category-actions">
          <h5><%= launch.name %></h5>
          <div class="actions">
          <a class="buttonlike" href="/launches/<%= launch.id %>">Info</a>
          <a class="buttonlike" href="/launches/<%= launch.id %>/edit">Edit</a>
          </div>
        </div>
      </div>
    <% }) %>
  </div>

 

  <%- include('../partials/end') %>

